
from AbsAlg8 import *

# To be used in the context of AbsAlg8

s = Variable('s') #statement
s1 = Variable('s1')
s2 = Variable('s2')

v = Variable('v') #variable

p = Variable('p') # pairing
p1 = Variable('p1')
p2 = Variable('p2')

n = Variable('n') # (natural) number
z = Variable('n0') #natural zero

#UniqueExistence
#forall[*w,x,y]s(*w, x) ^ s(*w, y) <=> E(x,y)
def mlExUn(statement, cVar):
	Forall(EV

EV = openSta('Eval', (s, p))









































